apiVersion: v1
kind: Service
metadata:
  name: haisin_redis
  labels:
    app: haisin_redis
spec:
  type: NodePort
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: haisin_redis
    tier: redis
