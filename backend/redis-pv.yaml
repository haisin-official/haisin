apiVersion: v1
kind: PersistentVolume
metadata:
  name: haisin-redis-pv
  labels:
    type: local
    app: haisin-redis
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data/haisin/redis"
  persistentVolumeReclaimPolicy: Retain
